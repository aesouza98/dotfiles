# Menus
bindd = SUPER, SPACE, Launch apps, exec, wofi --show drun
# bindd = SUPER, SPACE, Launch apps, exec, walker -p "Start…"
bindd = SUPER CTRL, E, Emoji picker, exec, walker -m Emojis
bindd = SUPER ALT, SPACE, Omarchy menu, exec, omanano-menu
bindd = SUPER, ESCAPE, Power menu, exec, omanano-menu system
bindld = , XF86PowerOff, Power menu, exec, omanano-menu system
bindd = SUPER, K, Show key bindings, exec, omanano-menu-keybindings
bindd = , XF86Calculator, Calculator, exec, gnome-calculator

# Aesthetics
bindd = SUPER SHIFT, SPACE, Toggle top bar, exec, omanano-toggle-waybar
bindd = SUPER CTRL, SPACE, Next background in theme, exec, theme-bg-next
bindd = SUPER SHIFT CTRL, SPACE, Pick new theme, exec, omanano-menu theme
bindd = SUPER, BACKSPACE, Toggle window transparency, exec, hyprctl dispatch setprop "address:$(hyprctl activewindow -j | jq -r '.address')" opaque toggle

# Notifications
bindd = SUPER, COMMA, Dismiss last notification, exec, makoctl dismiss
bindd = SUPER SHIFT, COMMA, Dismiss all notifications, exec, makoctl dismiss --all
bindd = SUPER CTRL, COMMA, Toggle silencing notifications, exec, makoctl mode -t do-not-disturb && makoctl mode | grep -q 'do-not-disturb' && notify-send "Silenced notifications" || notify-send "Enabled notifications"

# Toggle idling
bindd = SUPER CTRL, I, Toggle locking on idle, exec, omanano-toggle-idle

# Restart applications
bindd = SUPER CTRL, W, Toggle locking on idle, exec, omanano-restart-app waybar

# Toggle nightlight
bindd = SUPER CTRL, N, Toggle nightlight, exec, omanano-toggle-nightlight

# Screenshots
bindd = , PRINT, Screenshot of region, exec, omanano-cmd-screenshot
bindd = SHIFT, PRINT, Screenshot of window, exec, omanano-cmd-screenshot window
bindd = CTRL, PRINT, Screenshot of display, exec, omanano-cmd-screenshot output

# Screen recordings
bindd = ALT, PRINT, Screen record a region, exec, omanano-cmd-screenrecord region
bindd = ALT SHIFT, PRINT, Screen record a region with audio, exec, omanano-cmd-screenrecord region audio
bindd = CTRL ALT, PRINT, Screen record display, exec, omanano-cmd-screenrecord output
bindd = CTRL ALT SHIFT, PRINT, Screen record display with audio, exec, omanano-cmd-screenrecord output audio

# Color picker
bindd = SUPER, PRINT, Color picker, exec, pkill hyprpicker || hyprpicker -a

# File sharing
bindd = CTRL SUPER, S, Share, exec, omanano-menu share
